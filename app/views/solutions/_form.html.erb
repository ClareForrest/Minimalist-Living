<%= form_with model: solution, url: problem_solution_path(problem_id), local: true do |f| %>
  <% if solution.errors.any? %>
    <div class="row" id="error_explanation">
    <h2><% pluralize(solution.errors.count, "error") %> prohibited this article from being saved:</h2>
    <ul>
      <% solution.errors.full_messages.each do|msg| %>
        <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
  <% end %>
  
<%= f.label :title %>
<%= f.text_field :title %>
<%= f.label :body %>
<%= f.text_area :body %>
<%= f.file_field :image, accept: 'image/png, image/jpg, image/jpeg' %>
<p> PDF <%= f.file_field :document, accept: 'document/pdf' %></p>
<%= f.number_field :price %>

<%= f.submit %>
<% end %>