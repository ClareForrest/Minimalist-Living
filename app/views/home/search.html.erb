<%= form_with(scope: :search, url: problems_path, method: 'get', local: true) do |f| %>
<%= f.label :category %>
<%= f.collection_select :category, Problem.all, :id, :title, {}, {multiple: true} %>
<%= f.submit 'Search' %>
<% end %>